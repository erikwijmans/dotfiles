alias grep='grep --color=auto'
alias ls='ls -G'
alias cp='cp -v'
alias mv='mv -v'

addpath () {
    if [[ -d "$1" ]] &&  [[ ":$PATH:" != *":$1:"* ]]
    then
        export PATH="$1:$PATH"
    fi
}

local_alises="./zsh_aliases.local"
if [[ -f "${local_alises}" ]]; then
    . ${local_alises}
fi
